import { defineConfig } from '../src/command/template/util.js'

export default defineConfig({
    // 需要动态替换文本的文件
    parseFiles: [],
    templates: [
        {
            npmName: '',
        }
    ]
})